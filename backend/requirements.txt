# Backend API (Django + DRF) dependencies. Installed on top of the gtm engine
# (pip install -e .. from repo root) inside the container / venv.
# CI pins exact versions via backend/requirements.lock
# (regenerate: uv pip compile backend/requirements.txt -o backend/requirements.lock).
Django>=5.0
djangorestframework>=3.15
django-cors-headers>=4.3
dj-database-url>=2.1
gunicorn>=22.0
psycopg[binary]>=3.1
celery>=5.3
redis>=5.0
